import { anyValue } from "@nomicfoundation/hardhat-chai-matchers/withArgs"
import { expect } from "chai"
import hre from "hardhat"

describe("DividendSplitter", function () {
    describe("Deployment", function () {
        before(async () => {})

        beforeEach(async () => {})

        it("test", async function () {})
    })
})
